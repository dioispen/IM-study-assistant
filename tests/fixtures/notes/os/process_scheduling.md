# Process Scheduling

## Round Robin

Round robin scheduling assigns each process a fixed time slice called a quantum. When a process's quantum expires, the scheduler preempts it and moves it to the back of the ready queue so the next waiting process can run.
