# 傳輸控制協定

## 三向交握

用戶端先送出 SYN 封包，並在其中帶上自己的初始序號。伺服端收到之後回覆 SYN-ACK，一面確認對方的序號，一面帶上自己的初始序號。用戶端最後再送出一個 ACK，交握到此完成，雙方都確認了彼此的收送能力正常，連線才進入已建立狀態。

## 連線終止

關閉連線採用四次揮手。任一端送出 FIN 之後仍然可以繼續接收資料，直到雙方都送出並確認 FIN 為止。
